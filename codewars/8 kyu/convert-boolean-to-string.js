// Implement a function which convert the given boolean value into its string representation.
// Note: Only valid inputs will be given.

// Input Validation: The problem statement mentions that only valid inputs will be given, so you can assume that the input b will be a boolean.
// String Representation: The goal is to convert the boolean value into its string representation. JavaScript has two possible boolean values: true and false. You need to convert these values into the strings "true" and "false".
// Return Statement: The function should return the string representation of the boolean input.

function booleanToString(b){
// Convert boolean value to its string representation
  return b ? "true" : "false";
  }